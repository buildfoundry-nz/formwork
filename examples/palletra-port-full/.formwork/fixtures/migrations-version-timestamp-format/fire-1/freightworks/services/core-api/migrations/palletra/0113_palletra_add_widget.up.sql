-- A NEW migration minted with the banned sequential 0107-collision shape:
-- a 4-digit prefix above the frozen high-water instead of a 14-digit timestamp.
CREATE TABLE palletra.widget (id uuid PRIMARY KEY);
