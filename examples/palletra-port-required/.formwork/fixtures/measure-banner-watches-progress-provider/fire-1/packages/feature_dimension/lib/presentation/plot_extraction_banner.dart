// Progress source removed: the banner has no live progress to show.
class MeasureParseBanner extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Text('Processing');
  }
}
