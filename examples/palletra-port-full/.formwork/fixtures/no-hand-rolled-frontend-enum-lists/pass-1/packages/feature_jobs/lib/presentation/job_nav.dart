class TaskNav {
  // UI config-object list: first element is a widget constructor, not an enum
  // value, so it must not trip the gate.
  static const items = [_MenuEntry(label: 'Home'), _MenuEntry(label: 'Jobs')];
}
