import 'package:flutter/widgets.dart';
import 'package:plt_widgets/plt_widgets.dart';

class ProjectsPanel extends StatelessWidget {
  const ProjectsPanel();

  @override
  Widget build(BuildContext context) =>
      const BlankStateView(message: 'No projects yet');
}
