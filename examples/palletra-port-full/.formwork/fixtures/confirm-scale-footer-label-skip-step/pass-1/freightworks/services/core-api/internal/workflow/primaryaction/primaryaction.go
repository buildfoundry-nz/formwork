//go:build ignore

package primaryaction

// Frozen canonical footer copy (W-LABELS). primaryaction.go owns this copy.
const (
	labelScaleConfirm = "Confirm Scale"
	labelAccept       = "Approve"
	labelSkipAction   = "Skip"
)
