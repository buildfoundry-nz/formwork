# Ported from Palletra scripts/check-sku-line-gates-guarded.sh (PH-5 #8147),
# RULE A (template gate / resolve half). See sku-line-authored-gate-detects
# for the split rationale. This asserts the template add-to-bom gate (#8448)
# references ResolveSkuLineConflict.
rules:
  - id: sku-line-template-gate-resolves
    type: required-pattern
    scope:
      include:
        - "freightworks/services/core-api/routes/extraction/skus_add_to_bom.go"
    preprocess: decomment-go
    params:
      pattern: 'ResolveSkuLineConflict'
      mode: exists
    cure: "The template add-to-bom sku-line gate must call ResolveSkuLineConflict — enforce the estimator's resolution before creating a line (PH-5 #8147)."
    origin: scripts/check-sku-line-gates-guarded.sh
