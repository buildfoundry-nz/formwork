# TDD workflow

The RED/GREEN cycle used to be demonstrated in the legacy React client. See the
shared base API client at `client/src/lib/api/base.ts` <!-- want: governance-docs-paths-must-exist --> for the interceptor pattern to mirror.
