// A resurrected TypeScript file from the deleted React reference. The app is
// Flutter (Dart) + Go now — any .ts anywhere is a hard fail, no path exemption.
export function buildReport(rows: number[]): number {
  return rows.reduce((a, b) => a + b, 0);
}
