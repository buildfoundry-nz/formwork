import 'package:flutter/widgets.dart';

class _AlignToolState extends State<AlignTool> {
  final BackoffBudgetPolicy _retry;
  _AlignToolState(this._retry);

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
