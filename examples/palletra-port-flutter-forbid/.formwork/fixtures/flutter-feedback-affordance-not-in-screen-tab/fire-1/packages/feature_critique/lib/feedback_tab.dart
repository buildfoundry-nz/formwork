import 'package:flutter/material.dart';

// The in-screen affordance the gate makes un-reintroducible.
class CritiqueTab extends StatelessWidget { // want: flutter-feedback-affordance-not-in-screen-tab
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
