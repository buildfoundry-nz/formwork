import 'package:flutter_test/flutter_test.dart';

void main() {
  test('streams the live-page-ready scale field', () {
    expect(1 + 1, 2);
  }, skip: 'flaky, see #1034'); // want: dart-tests-forbid-skip-marker
}
