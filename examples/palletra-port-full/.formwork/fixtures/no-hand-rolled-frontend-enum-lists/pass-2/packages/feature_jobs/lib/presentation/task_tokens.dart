class TaskTokens {
  // Carved out by except.paths: **/presentation/task_tokens.dart is a UI badge
  // token catalog, not a server-validated business enum — an enum-shaped list
  // here must NOT trip the gate.
  static const List<String> tagCodes = ['urgent', 'blocked'];
}
