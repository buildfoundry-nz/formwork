name: CI
on: [pull_request]
jobs:
  go-checks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/cache/restore@v4
        id: go-cache
        with:
          path: ~/.cache/go-build
          key: go-checks-${{ runner.os }}-${{ github.sha }}
