//go:build ignore

// Package demo owns the demo widget assembly: it maps server SampleState into
// the three render slots and enforces the read-only display contract. Entry
// point Build; governed by docs/demo-governance.md.
package demo
